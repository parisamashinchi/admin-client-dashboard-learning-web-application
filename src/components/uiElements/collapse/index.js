import { Collapse, Panel as StyledPanel } from "./style";
import withDirection from "utils/withDirection";

export default withDirection(Collapse);

const Panel = withDirection(StyledPanel);
export { Panel };
